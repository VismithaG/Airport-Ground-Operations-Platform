// Work order list page (copied/merged from airport project)
import 'package:flutter/material.dart';

class WorkOrderList extends StatelessWidget {
	const WorkOrderList({super.key});

	@override
	Widget build(BuildContext context) {
		return const Scaffold(body: Center(child: Text('Work Order List')));
	}
}
