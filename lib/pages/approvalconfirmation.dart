// Placeholder copied from airport project: approval confirmation
import 'package:flutter/material.dart';

class ApprovalConfirmation extends StatelessWidget {
	const ApprovalConfirmation({super.key});

	@override
	Widget build(BuildContext context) {
		return const Scaffold(
			body: Center(child: Text('Approval Confirmation')),
		);
	}
}
